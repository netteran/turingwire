# Secrets & local env
.env
.env.local
.env*.local
*.log

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Node
node_modules/
package-lock.json

# Next.js
/.next/
/out/
next-env.d.ts
tsconfig.tsbuildinfo
.vercel

# Pipeline scratch files. Each Ingest run writes these to the runner and
# hands them to the next stage; the durable state lives in Postgres.
_data/staging_articles.json
_data/deduped_articles.json
_data/classified_articles.json
_data/run_log.json
_data/review_queue.json
_data/backlog.json
# Regenerated every run by generate_scraped_feeds.py, consumed by
# fetch_feeds.py in the same run — never a durable artifact.
_data/scraped_feeds/

# OS
.DS_Store
Thumbs.db
